# Installer for the alloy linux distribution.

# Some info on alloy linux
Alloy Linux is a nix-based distribution, it was made in the thought of having simplicity and usability for everyone.
Instead of having a config.nix file, alloy uses a GUI app store, instead of a configuration file.

